import MDXLayout from '../../components/MDX/Layout/Index'
import MDXContentSection from '../../components/MDX/Content/Index'
import MDXToCSection from '../../components/MDX/TOC/Index'
import MDXContentBlock from '../../components/MDX/ContentBlock'
import CodeSyntax from '../../components/MDX/CodeSyntax/Index'

import { TYPESCRIPT_CONSTS, TYPESCRIPT_COMPONET_TYPE } from '../../data/MDX_CODE'
import FooterRouter from '../../components/FooterRouter'
import Text from '../../components/Text/Index'
import { TOC_TYPESCRIPT } from '../../data/TABLE_CONTENTS'

<MDXLayout>
    <MDXContentSection>

      <Text variant="heading3" color="white" id="start">Typescript</Text>
      <MDXContentBlock >
        All WalletConnect / Web3Modal packages support typescript out of the box.
        But there are few unique caveats that you might run into that we discuss below.
      </MDXContentBlock>

      <Text variant="heading4" color="white" id="useConst">Using consts in config object</Text>
      You need to use `as const` on some values, so that typescript doesn't see them as plain `string` type.
      <MDXContentBlock>
        <CodeSyntax codeString={TYPESCRIPT_CONSTS} codeType={"app"} />
      </MDXContentBlock>

      <Text variant="heading4" color="white" id="componentType">Add webc omponent types</Text>
      Some frameworks require custom type definitions for custom dom elements / web components.
      We define these for our `@web3modal/react` package, however if you don't use it or are in standalone mode, you may need to declare them globally yourself.
      Below is example of how we do it for react.
      <MDXContentBlock>
        <CodeSyntax codeString={TYPESCRIPT_COMPONET_TYPE} codeType={"app"} />
      </MDXContentBlock>

      <FooterRouter
        previousRoute="/get-started"
        previousRouteName="Get Started"
        padding="lg"
      />
    </MDXContentSection>
    <MDXToCSection data={TOC_TYPESCRIPT}/>

</MDXLayout>
