import styles from '../../styles/Home.module.css'
import MDXLayout from '../../components/MDX/Layout/Index'
import MDXContentSection from '../../components/MDX/Content/Index'
import MDXToCSection from '../../components/MDX/TOC/Index'
import MDXContentBlock from '../../components/MDX/ContentBlock'
import CodeSyntax from '../../components/MDX/CodeSyntax/Index'

import { INSTALL_REACT_DEPENDENCIES, CONFIGURE_REACT } from '../../data/MDX_CODE'
import FooterRouter from '../../components/FooterRouter'
import Text from '../../components/Text/Index'
import { TOC_REACT } from '../../data/TABLE_CONTENTS'

<MDXLayout>
    <MDXContentSection>

      <Text variant="heading4" color="white" id="start">Getting Started</Text>
      <MDXContentBlock >
        Please ensure you are updated to the latest React / Next.js or similar version, as web3modal libraries target es2020.
        Web3Modal hooks are based on [wagmi](https://wagmi.sh), so it is good idea to read through their documentation as well.
        Do note, you have to import and use these hooks from @web3modal packages.
      </MDXContentBlock>

      <Text variant="heading4" color="white" id="install">1. Install Dependencies</Text>
      <MDXContentBlock >
        <CodeSyntax codeString={INSTALL_REACT_DEPENDENCIES} codeType={"terminal"} />
      </MDXContentBlock>

      <Text variant="heading4" color="white" id="configure">2. Configure Web3Modal at the root of your app</Text>
      <MDXContentBlock >
        <CodeSyntax codeString={CONFIGURE_REACT} codeType={"app"} />
      </MDXContentBlock>

      <FooterRouter
            previousRoute="/get-started"
            previousRouteName="Get Started"
            nextRoute="/react-hooks"
            nextRouteName="Hooks"
            padding="lg"
        />
    </MDXContentSection>
    <MDXToCSection data={TOC_REACT}/>

</MDXLayout>
