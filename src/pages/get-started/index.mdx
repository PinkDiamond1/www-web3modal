import styles from '../../styles/Home.module.css'
import MDXLayout from '../../components/MDX/Layout/Index'
import MDXContentSection from '../../components/MDX/Content/Index'
import MDXToCSection from '../../components/MDX/TOC/Index'
import MDXContentBlock from '../../components/MDX/ContentBlock'
import CodeSyntax from '../../components/MDX/CodeSyntax/Index'

import { INSTALL_INSTRUCTIONS, GET_CONNECTED_REACT, APP_SETUP } from '../../data/MDX_CODE'
import { TOC_GET_STARTED } from '../../data/TABLE_CONTENTS'
import FooterRouter from '../../components/FooterRouter'
import Text from '../../components/Text/Index'

<MDXLayout>
  <MDXContentSection>
      <Text variant="heading4" color="white" id="get-started">Get Started</Text>
      <MDXContentBlock >
        Getting started with Web3Modal is fast and easy. Web3Modal is chain agnostic and plays with your preferred front-end framework.
        To see what other frameworks and chains we currently support, please visit our [GitHub](https://github.com/WalletConnect/web3modal).
      </MDXContentBlock>

      <Text variant="heading5" color="white" id="wallet-connect-cloud">WalletConnect Cloud</Text>
      <MDXContentBlock>
        If this is your first time using Web3Modal, you will need to obtain a `projectId`. You can follow [this guide](./cloud-explorer/get-project-id) if you need help.
      </MDXContentBlock>

      <Text variant="heading5" color="white" id="install">Install</Text>
      <MDXContentBlock>
        Install the packages for your project. In our example, our front-end framework will be
        React and the chain will be Ethereum.
        <CodeSyntax codeString={INSTALL_INSTRUCTIONS} codeType={"terminal"} />
      </MDXContentBlock>

      <Text variant="heading5" color="white" id="app-setup">App Setup</Text>
      <MDXContentBlock>
        In the consuming app:
        - Import ```Web3Modal``` from ```@web3modal/react```.
        - Create a ```config``` object.
        - Set ```projectId``` to ```NEXT_PUBLIC_PROJECT_ID```.
        <CodeSyntax codeString={APP_SETUP} codeType={"app"} />
      </MDXContentBlock>
      <Text variant="heading5" color="white" id="get-address">Get Address</Text>
      <MDXContentBlock>
        To get the connected wallet’s address, `import ConnectButton and useAccount from @web3modal/react.`
        `isConnected` is a boolean that will return `true` or `false` if there is a wallet
        connected. address is the connected wallet’s address.
        <CodeSyntax codeString={GET_CONNECTED_REACT} codeType={"app"} />
      </MDXContentBlock>

      <FooterRouter previousRoute="/try-it-out" previousRouteName="Try It Out" nextRoute="/react" nextRouteName="React" padding="lg" />

  </MDXContentSection>
  <MDXToCSection data={TOC_GET_STARTED}/>
</MDXLayout>
