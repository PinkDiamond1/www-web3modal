import styles from '../../styles/Home.module.css'
import MDXLayout from '../../components/MDX/Layout/Index'
import MDXContentSection from '../../components/MDX/Content/Index'
import MDXToCSection from '../../components/MDX/TOC/Index'
import CodeSyntax from '../../components/MDX/CodeSyntax/Index'
import { INSTALL_INSTRUCTIONS, GET_ADDRESS, APP_SETUP } from '../../data/MDX_CODE'
import FooterRouter from '../../components/FooterRouter'
import Text from '../../components/Text/Index'

<MDXLayout>
  <div style={{display: "flex"}}>
    <MDXContentSection>
        <div>
          <div style={{padding: '1em 0 1em 0'}}>
            # Get Started
            <Text variant="text3" color="grey">
              Getting started with Web3Modal is fast and easy. Web3Modal is chain agnostic and plays with your preferred front-end framework.
              To see what other frameworks and chains we currently support, please visit our [GitHub](https://github.com/WalletConnect/web3modal).
            </Text>
        </div>

          <div style={{ padding: '1em 0 1em 0' }}>
            ## WalletConnect Cloud
            <Text variant="text3" color="grey">
              If this is your first time using Web3Modal, you will need to obtain a
              projectId. You can follow [this guide](https://walletconnect.com/) if you need help.
            </Text>
          </div>

          <div style={{ padding: '1em 0 1em 0' }}>
            ## Install
            <Text variant="text3" color="grey">
              Install the packages for your project. In our example, our front-end framework will be
              React and the chain will be Ethereum.
            </Text>
            <CodeSyntax codeString={INSTALL_INSTRUCTIONS} />
          </div>

          <div style={{ padding: '1em 0 1em 0' }}>
            ## App Setup
            <Text variant="text3" color="grey">
              In the consuming app - Import ```Web3ModalProvider``` from ```@web3modal/react``` -
              Import chains and provider from ```@web3/ethereum```. - Wrap the component with
              ```Web3ModalProvider```. - Set ```projectId``` to ```NEXT_PUBLIC_PROJECT_ID``` - Set chains and
              providers according to the example code below.
            </Text>

            <CodeSyntax codeString={APP_SETUP} />
          </div>

          <div style={{ padding: '1em 0 1em 0' }}>
            ## Get Address
            <Text variant="text3" color="grey">
              To get the connected wallet’s address, import ConnectButton and useAccount from
              @web3modal/react. connected is a boolean that will return true or false if there is a wallet
              connected. address is the connected wallet’s address.
            </Text>
          </div>
          <FooterRouter previousRoute="/try-it-out" previousRouteName="Try It Out" padding="lg" />
        </div>
    </MDXContentSection>
    <MDXToCSection/>

  </div>
</MDXLayout>
