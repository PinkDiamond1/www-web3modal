import styles from '../../styles/Home.module.css'
import MDXLayout from '../../components/MDX/Layout/Index'
import MDXContentSection from "../../components/MDX/Content/Index"
import MDXToCSection from "../../components/MDX/TOC/Index"

<MDXLayout>
  
<div style={{display: "flex"}}>
  <MDXContentSection>
      <div>
<div style={{padding: '1em 0 1em 0'}}>
    # Getting Started
    Getting started with Web3Modal is fast and easy. Web3Modal is chain agnostic and plays with your preferred front-end framework.
    To see what other frameworks and chains we currently support, please visit our [GitHub](https://github.com/WalletConnect/web3modal).
  </div>

  <div style={{padding: '1em 0 1em 0'}}>
    ## WalletConnect Cloud
    If this is your first time using Web3Modal, you will need to obtain a projectId. You can follow [this guide](https://walletconnect.com/) if you need help.
  </div>

  <div style={{padding: '1em 0 1em 0'}}>
    ## Install
    Install the packages for your project. In our example, our front-end framework will be React and the chain will be Ethereum.

    <div style={{background: '#3B4040', padding: "15px 20px 15px 20px", borderRadius: "0 0px 20px 20px"}}>
      ```
      npm install @web3modal/react @web3modal/ethereum ethers
      ```
    </div>

  </div>

  <div style={{padding: '1em 0 1em 0'}}>
    ## App Setup
      In the consuming app
      - Import ```Web3ModalProvider``` from ```@web3modal/react```  
      - Import chains and provider from ```@web3/ethereum```.
      - Wrap the component with ```Web3ModalProvider```.
      - Set ```projectId``` to ```NEXT_PUBLIC_PROJECT_ID``` 
      - Set chains and providers according to the example code below.

    <div style={{background: '#3B4040', padding: "15px 20px 15px 20px", borderRadius: "0 0px 20px 20px"}}>
       ```
      import { chains, providers } from '@web3modal/ethereum'
      import { Web3ModalProvider } from '@web3modal/react'

      const NEXT_PUBLIC_PROJECT_ID = { process.env }

      const modalConfig = {
        projectId: NEXT_PUBLIC_PROJECT_ID,
        theme: 'dark',
        accentColor: 'default',
        ethereum: {
          appName: 'web3Modal',
          autoConnect: true,
          chains: [chains.mainnet],
          providers: [providers.walletConnectProvider({ projectId: NEXT_PUBLIC_PROJECT_ID })]
        }
      }

      export default function App({ Component, pageProps }) {
        return (
          <Web3ModalProvider config={modalConfig}>
            <Component {...pageProps} />
          </Web3ModalProvider>
        )
      }
      ```
    </div>

     
    </div>
  
  <div style={{padding: '1em 0 1em 0'}}>
    ## Get Address
    Lorem ipsum dolor sit amet, consectetur adipiscing elit. Maecenas pellentesque vitae magna sit amet fringilla.
    <div style={{background: '#3B4040', padding: "15px 20px 15px 20px", borderRadius: "0 0px 20px 20px"}}>
      ```
      import { ConnectButton, useAccount } from '@web3modal/react'
        export default function HomePage() {
          const { connected, address } = useAccount()

          return connected ? (
            <>
              <h1>{address ? address : 'none'} </h1>
            </>
          ) : (
            <ConnectButton />
          )
      }      
      ```
    </div>

  </div>

  </div>
  </MDXContentSection>

  <MDXToCSection/>
</div>





  
</MDXLayout>
